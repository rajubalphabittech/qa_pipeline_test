pipeline {
    agent any 

    stages {
        stage('Build') { 
            steps { 
                sh 'ls -l' 
            }
        }
        stage('Test'){
            steps {
                sh 'date'
            }
        }
    }
}
